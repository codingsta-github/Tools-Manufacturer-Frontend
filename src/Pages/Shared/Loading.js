import React from 'react';

const Loading = () => {
    return (
        <div>
            <button class="btn btn-square loading m-auto flex justify-center items-center"></button>
        </div>
    );
};

export default Loading;